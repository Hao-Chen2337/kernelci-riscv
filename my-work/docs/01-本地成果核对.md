# 01 本地成果核对（事实卡）

> 只读核对 kernelci-pipeline 工作区 + my-work 现有产物。全部引用精确到 file:line；无法从本地证据确认的均标「未验证/仓库内无原始日志」。

**TL;DR**
1. worker 实测 779 行（`wc -l`），29 个函数；纯代码 605 行、整行注释 34 行、docstring 文本 49 行、空行 90 行——总结 §7 声称「777 行 / 代码约 650 / 注释 docstring 32 行」**与现文件不一致**，只可作粗粒度参考。
2. §7 的子系统行数（PTY ~170 / 健壮性 ~180 / 三方法+ext4 ~150 / 协议+CLI ~150）**无法与代码作一一对应的独立核算**：健壮性代码分散在多个函数内，非连续区段。
3. 64 行配置 diff 属实（5 文件纯新增，platforms 10 / jobs 18 / pipeline 8 / scheduler 27 / pyproject 1），与总结 §1 描述逐条吻合。
4. **仓库内没有支撑「9/9、7ok/2skip、6s/27s/167s、ssnpm=true」的原始 QEMU 运行日志**；trend-*.md 是 K3 真机（X100）数据（riscv 7/1/0、KVM 3/17），与 QEMU 声称数值不是同一批运行。唯一 QEMU 日志证据是 kernelci-api docker storage 里 6.18.0 时代的 8 个 test 节点产物。
5. QEMU 11.1.1 二进制与源码**本地存在且版本已验证**（`/home/hao/qemu-install-11`、`/home/hao/qemu-11.1.1`），但无配套运行日志。

---

## 1. worker 精确账本（tools/riscv_pull_worker.py，779 行）

按「下载/解包/烘焙 → QEMU 驱动 → 三套测试方法 → 协议/回传 → 健壮性/主流程」分块（span=原始行含注释空行，code=AST 判定的纯代码行）：

| 子系统 | 函数（file:line 起始） | span | code |
|---|---|---|---|
| 头/imports/常量/markers/KVM 子集 | L1–76（`KVM_TEST_SUBSET` L62–72、`DEFAULT_PROFILE` L48–53） | 76 | 41 |
| 下载+校验+安全解包 | `download` L77 / `download_kernel` L100 / `safe_members` L114 / `extract_tarball` L135 | 77 | 58 |
| ext4 烘焙 + mount hack | `bake_rootfs_image` L148 / `mount_cmd` L182 / `_MOUNT_PROC/_SYS` L196–197 | 52 | 35 |
| QEMU argv + guest 引导串 | `qemu_cmd` L200 / `guest_bootstrap` L214 | 25 | 20 |
| PTY 串口驱动（读/登出/drain） | `serial_reader` L229 / `drain_pty` L266 | 53 | 46 |
| run_qemu（select+threading+杀 QEMU） | `run_qemu` L286 | 58 | 48 |
| 三套测试方法 | `baseline_qemu` L346 / `kselftest_qemu` L365 / `kselftest_kvm_qemu` L382 | 62 | 50 |
| pull_labs 协议（poll/取定义/回传/error body） | `pollevents` L414 / `retrieve_job_definition` L425 / `post_result` L432 / `error_body` L456 | 57 | 41 |
| TAP 汇总 + job 调度 | `summarize_kselftest` L467 / `summarize_boot` L559 / `run_job` L492 / `_report_missing` L516 / `_run_job_in_workspace` L523 / `pick_driver` L566 | 154 | 130 |
| 健壮性：事件处理/失败计数 | `handle_event` L621（seen 判定 L717、失败≥3 放弃 L651） | 34 | 29 |
| 状态持久化/flock | `save_state` L657 / `load_state` L664 / `poll_loop` L671（flock L677–683、游标仅整批成功才推进 L714/726–729、seen 上限 1000 L723） | 77 | 67 |
| CLI/主流程 | `main` L736（argparse L737–768、profile 组装 L769–774） | 44 | 40 |

**全局口径（AST）**：779 = code 605 + 整行注释 34 + docstring 文本 49（含 docstring 引号 50）+ 空行 90；另带行尾注释 10 行。函数共 29 个 `def`。

**三套方法的 guest 命令与参数（供综合代理直接引用）**：
- baseline（L346–362）：initramfs cpio.gz（无 root=），`append=console=ttyS0 earlycon=sbi`，guest 仅 `echo ===BOOT_""OK===`（引号防回显撞车，L40–42 注释）。
- kselftest（L365–379）：ext4 盘 `root=/dev/vda rw init=/bin/sh`；`guest_bootstrap`（L214–224）先挂 devtmpfs/proc/sys，再 `sh run_kselftest.sh -c {collections}`，结束回显 `===KSELFTEST_""DONE===`。
- kselftest-kvm（L382–407）：cpu 追加 `,h=true`（L392–393），guest `insmod $(find /lib/modules -name kvm.ko…)` + `run_kselftest.sh -t kvm:<t>`，子集 = `KVM_TEST_SUBSET`（L62–72 九项）或 `--kvm-tests`/job `subset`（L595–596）。

**对照总结 §7（总结文档 L164）**：声称「~170 PTY / ~180 健壮性 / ~150 三方法+ext4 / ~150 协议+CLI / 注释 32 / 最终 777 行、代码约 650」。实测：文件 **779 行**（非 777）、纯代码 **605**（非 ~650）、整行注释 34 + docstring 49（无法还原「32 行」口径）；子系统按上表为 **PTY+run_qemu 111、健壮性相关分散函数合计约 160（span）、三方法 62、协议+调度 211（span）**。结论：**声称的拆分口径不可独立复现**，只能当量级估计；总行数差 2 行可能与 docstring 计算方式或最后改动有关（worker mtime 09-07 12:05 晚于总结写作 12:09 之前的版本差异，未验证）。

## 2. 64 行配置 diff 逐文件核实

`git diff --stat`：5 文件、**64 insertions、0 deletions**（`baseline-riscv64-qemu` 等旧名在新 jobs.yaml 无残留）。

| 文件 | 新增 | 内容与总结 §1 对照 |
|---|---|---|
| config/platforms.yaml（L577–586 区） | 10 | `qemu-riscv64: <<: *qemu-device; arch: riscv; context: cpu rv64,v=true; machine virt; memory 4G; guestfs_interface virtio`——与声称一致（对照 qemu-arm64 L575–583 的模板形态） |
| config/jobs-pull-labs.yaml（L227 起） | 18 | `baseline-riscv-pull-labs: *baseline-job-pull-labs`（锚点 L8，ramdisk 走 `{brarch}`）；`kselftest-riscv-pull-labs`（collection: riscv）与 `kselftest-kvm-riscv-pull-labs`（collection: kvm）复用 `*kselftest-params-pull-labs` 锚点（L41，nfsroot trixie-kselftest `{debarch}`）——一致 |
| config/scheduler-pull-labs.yaml（L227–253） | 27 | 3 条目共享 `&kbuild-gcc-14-riscv-node-event`（`<<: *node-event-kbuild` + name kbuild-gcc-14-riscv，L228–231）与 `&pull-labs-riscv-runtime`（pull_labs/pull-labs-riscv，L232–234）；platforms 均 qemu-riscv64——一致 |
| config/pipeline-pull-labs.yaml（尾部） | 8 | runtime `pull-labs-riscv`：lab_type pull_labs、poll_interval 45、timeout 7200、callback token `kernelci-pull-labs-riscv`——一致 |
| pyproject.toml（L26） | 1 | `"tools/riscv_pull_worker.py" = ["C901"]`，与 example_pull_lab(_pytest).py 同待遇——一致 |

「riscv64-kselftest-kernelci image 同时产出 kselftest tar 与 modules」的依据：config/jobs.yaml L1447–1455 `kbuild-gcc-14-riscv` 用 image `gcc-14:riscv64-kselftest-kernelci`；本地 docker 时代 kbuild 节点产物 metadata（`kernelci-api/docker/storage/data/kbuild-gcc-14-riscv-6a9d3091…/metadata.json`）确实列出 `Image.gz / modules.tar.xz / kselftest.tar.xz / kselftest_targets.txt`——**该 claim 有本地证据**（但那是 6.18.0 本地 kbuild，生产 v6.19.7-rc1 无本地验证）。

## 3. 证据核对：trend-*.md 与声称数值

| 声称（总结 §1/README） | 本地证据 |
|---|---|
| riscv kselftest QEMU「9/9、27s」、两次复跑 | ❌ 无 QEMU 运行日志。trend-riscv.md 只有 K3 真机 X100 两行（08-27，gcc/clang，kernel v6.18，**7/1/0**） |
| kvm kselftest QEMU「7 ok/2 skip、~150–167s」 | ❌ 无日志。trend-kvm.md 是 K3 真机（08-27，6.18.3-generic，**3/17**）；「与 K3 一致」可对应 trend-kvm 但数值不同批 |
| baseline「PASS、6s」 | 部分：kernelci-api docker storage 有 4 个 baseline 节点（如 `baseline-riscv64-qemu-6a9d1852…/qemu-test.log`，kernel 6.18.0）含 `===BOOT_OK===`/`/ #`；**无时长记录** |
| 「QEMU 11.1.1 + ssnpm=true」 | 部分：`/home/hao/qemu-install-11/bin/qemu-system-riscv64 --version` = **11.1.1（已实测）**；qemu-11.1.1 源码 + build-qemu.sh 头部注释声明 ssnpm 支持；**无 ssnpm=true 的运行日志**，ssnpm 效果为文档自述 |
| K3 真机对比结论 | ✅ trend-riscv/kvm + README「passed 7/failed 1」「KVM 3/17」互证；但 README 里「QEMU 7 pass 2 skip」无日志 |
| pointer_masking 61 子测试 59 过 2 SKIP | ❌ 无原始输出；docker 时代 6.18 日志无 pointer_masking 子计数 |

仓库内**唯一 QEMU 原始日志** = `kernelci-api/docker/storage/data/` 下 9 个 test 节点（job 名 `baseline-riscv64-qemu-*`×4、`kselftest-riscv64-*`×5，kernel 6.18.0，09-01/09-06），每节点仅一个 `qemu-test.log`：kselftest 日志显示 top-level 8 项（pointer_masking/hwprobe/cbo/which-cpus/run_mmap.sh/sigreturn/v_initval/vstate_prctl，**无 vstate_ptrace 结果**），09-06 17:30/17:31 两节点 8ok/0notok（pointer_masking 已修），09-01/09-06 15:40 三节点 6ok/2notok（pointer_masking 与 sigreturn not ok）。这些是**废弃 docker-QEMU 时代产物**（job 名非 pull-labs），不是现 worker 在 v6.19.7-rc1 上的运行。

## 4. 与 example_pull_lab.py（361 行）对照——worker 自研候选

example（tuxrun 路线）只有 6 个函数：`pollevents` L25（**state=done**，L29）、`retrieve_job_definition` L37、`parse_job_definition_url` L44、`run_tuxrun` L57（把 kernel/modules/rootfs URL 交给 `tuxrun --device --kernel --modules --rootfs --tests`）、`prepare_and_run` L116、`main` L165。**example 无任何结果回传/回调函数，也不解析 TAP**。

worker 中 **example+tuxrun 路线没有对应物（纯自研、理论上可被 tuxrun 外包取代）的函数**：
- 全部 QEMU 驱动：`qemu_cmd` L200、`guest_bootstrap` L214、`serial_reader` L229、`drain_pty` L266、`run_qemu` L286（example 不启动 QEMU，tuxrun 代跑）；
- 全部 rootfs/下载/解包/烘焙/mount：`download`/`download_kernel`/`safe_members`/`extract_tarball`/`bake_rootfs_image`/`mount_cmd`（tuxrun 自己拉 kernel/modules/rootfs 并引导）；
- 三套测试方法 `baseline/kselftest/kselftest_kvm_qemu`；
- 回传与解析：`post_result`/`error_body`/`summarize_kselftest`/`summarize_boot`（example 整块缺失）。

两侧有概念对应物但实现不同的：`pollevents`（worker L414 用 **state=available**，比 example 的 done 新）、`retrieve_job_definition`（同名）、`pick_driver`/`run_job`/`_run_job_in_workspace` ↔ example `prepare_and_run`、`poll_loop`/`main` ↔ example `main`（worker 额外有 seen/游标/flock/失败计数）。另 `example_pull_lab_pytest.py` L33–52 同样 state=done；`example_pull_lab_lava.py` L61–73 亦 state=done。

---

## 留给综合代理的关键判断/数字
1. **行数**：worker=779 行 / 605 纯代码 / 34 整行注释 / 49 docstring 文本；总结 §7 的 777、~650、32 行口径不成立（差 2 行 + 注释统计口径不符），引用时以本卡数值为准。
2. **worker 相对 example 的净自研量**：QEMU 驱动 111 行 + 下载/烘焙/mount 129 行 + 三方法 62 行 ≈ 300+ 行 span 是 tuxrun 可整体替代区；协议/健壮性 ~200 行 span 即使换 tuxrun 仍需保留（example 缺回传/TAP/幂等）。
3. **配置**：64 行 = platforms 10 + jobs 18 + scheduler 27 + pipeline 8 + pyproject 1；`kbuild-gcc-14-riscv` 事件、runtime `pull-labs-riscv`、token 名、锚点复用均与总结一致；「image 出 kselftest tar+modules」有本地 kbuild metadata 佐证（6.18 时代）。
4. **最弱证据点**：QEMU 9/9、7ok/2skip、6s/27s/167s、ssnpm=true、v6.19.7-rc1 生产产物——本地无任何运行日志，只有自述文档；trend-*.md 是 K3 真机且数值不同（riscv 7/1/0、KVM 3/17）。对外陈述需降级为「文档自述 + 本地 6.18 docker 时代 8/8 日志」，或补一次带 `--output-dir`/日志留存的复跑取证。
5. worker 中 summary_kselftest（L467–489）缺「per-test 明细」上报、top-level 8 vs 声称 9 的差异（vstate_ptrace）待综合代理解释/验证。
