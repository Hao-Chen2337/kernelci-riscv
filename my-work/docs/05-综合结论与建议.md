# 05 综合结论与建议(汇总 01–04)

> ⚠️ **2026-09-09 更正(防误用)**:本文写于"结果回收未接线"的旧前提之下。该前提已被 07 §5 推翻并线上证实——真实 lab 回传 **LAVA 兼容 body**,走已接线的 lava_callback.py(qemu-arm64 线上 1983 条 pass;本地端到端节点 6a9fb8bf…/6aa11653… done/pass 已实测)。因此本文第 33/57/68/117 行"回收待上游接线"类表述**全部作废**;PR1 描述的正确口径是"复用上游已通的结果回收路径"。其余结论(64 行配置=Phase 3 交付物、tracking issue 开在 kernelci-project、worker 留 lab 侧)不受影响。
> 本报告综合四份研究 memo(01 本地成果核对 / 02 issue49 归属 / 03 tuxrun 外包可行性 / 04 别人怎么写),并复核了 `tools/riscv_pull_worker.py` 关键段与总结文档。行数口径以 01 的 AST 账本为准。

## 1. TL;DR(硬结论)

1. **worker 779 行不是"思想有问题",而是"执行层越界"**:605 纯代码中约 300 行(下载/烘焙/mount L77–197 + QEMU/PTY L200–343 + 三方法 L346–407 的 span)是在重复 LAVA/tuxrun 已解决 12 年的领域;但协议+健壮性 ~300 行(state=available、幂等/flock/seen、回调回传、安全解包、TAP 解析)恰恰是上游 example 缺失的生产要素,应保留——**"太长"成立约四成,且都可删,不是推倒重写**。
2. **tuxrun 可以外包,卡点不在 QEMU 而在测试类与表达**:净省约 **290–330 行(37–42%)**,主要消灭 PTY/烘焙/guest 驱动;但 tuxlava 无 `kselftest-riscv` 测试类、KVM 的 insmod/H 扩展/精选子集无 CLI 表达、上游 example 仍是 state=done+input() 暂停——**三个前置均未就绪,当前节点不建议主路径切换**(详见 §3)。
3. **issue49 的"主仓交付物"严格等于 64 行配置(test profile suite)**,worker 属 lab 侧工具;四阶段里 Phase 1/2 技术上已达成(除 tracking issue),**真正缺的是三个行动:开 kernelci-project tracking issue、发 PR1(纯配置)、Phase 4 补账**——无遗留技术债。
4. **tracking issue 坐标 = kernelci/kernelci-project(先例 #579,同 SOW 两位实习生已共用),总结 §8 写的"kernelci-pipeline 开 issue"是笔误需修正**;dev-partners 只做程序侧回帖。
5. **当前交付≈Phase 2 完成 + Phase 3 代码就绪未提交**:Phase 1 只差 tracking issue 一条(纯运营,当天可闭环);Phase 3 差 PR1 提交与合入;Phase 4 未开始。
6. **最弱证据点**:QEMU"9/9、7ok/2skip、6s/27s/167s、ssnpm=true"在仓库内**无任何运行日志**,只有 6.18.0 时代废弃 docker-QEMU 产物与文档自述;对外陈述需降级或先补一次留日志的复跑。
7. **宣传口径红线**:勿写"上游第一个 riscv QEMU 测试"——legacy kernelci-core#2008(2023-07)已有先例;准确说法是"补齐 modular kernelci-pipeline 的 riscv QEMU kselftest 覆盖(pull_labs 形态)"。

## 2. 直接回答用户的三个疑问

### (a) 700 多行是不是太长 / 思想有问题 → **部分是;长的部分恰好全可删,不长的部分(协议+健壮性)删了才要命**

| 账本 | 行数 | 说明 |
|---|---|---|
| 全文件 | 779(605 纯代码 + 34 整行注释 + 49 docstring + 90 空行) | 29 个 def |
| ①协议层+健壮性(L410–775 为主 + 分散) | ~300 span | state=available 轮询、seen+flock+状态文件、整批成功才推游标、失败≥3 放弃、Content-Length+sha256、safe_members、回调 POST+error_body、TAP 汇总、CLI——**example 全缺,生产必需** |
| ②下载/解包/ext4 烘焙/mount(L77–197) | ~121 span(58+ code 块) | tuxrun/LAVA 代下载+注入 overlay,可删/半删 |
| ③QEMU/PTY 驱动(L200–343) | ~144 span(~114 code) | = LAVA shell.py/serial.py(pexpect)+AutoLogin 的复写,**最贵维护点,可整删** |
| ④三方法 builder(L346–407) | 62 span | 换 tuxrun 后改写为命令行参数,大幅缩水 |

对照:上游官方期待 lab 执行层 ≈0 行自研(connecting-pull-lab.md 只让你跑 example+tuxrun);三个 example 的执行层全部外包,协议层实测 150–300 行。**所以"779 vs example 361"的差 = ①(+~150,该加) + ②③④(约 300,该删/该外包)**。结论:不是"想太多写太长",是**把上游惯例上属于库/工具的执行层也手写了**,而这恰好是自述最难的 PTY/mount 部分——难不是因为你写得差,是因为你在重写 lava-dispatcher。删减估价:②③④ 合计约 290–330 行 span 可整体让位,占总行数 37–42%,其中 ~250 行是"维护最贵"的代码。

### (b) 为什么不能外包给 tuxrun / 能不能外包 → **能;外包后 779 → 约 450 行;卡点不在 QEMU**

外包边界(memo 03 §3–§4 实证,`tuxrun --list-devices/--list-tests` 本机实测):
- **能外包的**:串口/登录/杀 QEMU/===DONE=== 全部(LAVA lava-run 在 podman 容器内用 pexpect 完成,镜像 `linaro/tuxrun-dispatcher:latest` 自带 qemu-system-riscv64);下载缓存;`qemu-riscv64` 设备存在(machine=virt, cpu=rv64),`--parameters cpu=rv64,h=true` 可覆盖出 H 扩展。
- **卡点(均未就绪)**:①tuxlava 126 个 kselftest 类里**没有 `kselftest-riscv`**(`--list-tests` 实测缺);②KVM 定向(insmod kvm.ko + `h=true` + 9 项精选子集)无 CLI 表达,子集只能用 SKIPFILE 部分近似;③上游结果回收未闭环(lava_callback.py:525 硬编码 LAVA Callback)——这 3 条与执行器无关,任何形态都要面对。
- **形态与行数**:方案 A(worker 保留协议骨架,执行块换 `subprocess tuxrun`)**约 430–480 行**;方案 B(改 example_pull_lab.py 补 state=available/去 input()/加幂等+回调)**约 500–550 行**。两者都要补 tuxlava 的 kselftest-riscv 类(上游 PR,~15 行)+ KVM 专属表达。
- **保留的功能**(换 tuxrun 后仍在):state=available 轮询、平台/runtime 过滤、seen/状态文件/flock、整批游标、失败重试、回调 POST+error_body、sha256 校验、baseline 判定。
- **为什么不现在切**:收益集中在"消灭自研驱动维护"(对演示/验收价值小),代价是引入 podman+容器镜像依赖、等上游 tuxlava 发版、KVM 路径需重新验证;自研 worker 已实测可跑、协议正确(example 的 state=done 才是错的)。**结论:方向对、是上游钦定形态(长期待办 §8),但不作为本轮主路径;作为 PR 后置项评估。**

### (c) issue49 每一条是不是在 kernelci 里搞 → 归属表(Phase 1 优先)

| Phase | 条目 | 归属 | 用户现状 | 缺口 |
|---|---|---|---|---|
| 1 | containerized testing pipeline initialized | ②本地为主(官方 compose 非强制,措辞+xjysiiau 先例 #579) | ✅ my-work harness + pull_labs 架构 | 建议 runbook 补"官方栈本地跑通"佐证(未验证) |
| 1 | tracking issue board established with parent community | ① **kernelci/kernelci-project**(先例 #579;dev-partners 只回帖) | ❌ 未开 | **开 issue + #49 回帖挂链接(Phase 1 完成只差这条)** |
| 1 | first validation script parsed locally | ①门禁 = 仓库 tests/validate_yaml.py | ✅ 本地解析通过 | 无(输出贴 PR/issue 作证据) |
| 2 | Primary KernelCI execution logic deployed | ②部署 + ①配置侧(pull_labs runtime/job 由上游 scheduler 渲染) | ⏳ worker 实测跑通;scheduler 条目未合入 | PR1 合入后等上游接线(上游 future work) |
| 2 | drift 检测 / regression pass-rates / QEMU 定向回归 | ②本地工具 + ②实测数据 | ✅ config_drift.py 等 + 声称全绿 | 数据进 runbook/blog;**PR 前把 src/ 下 3 个自研文件收走** |
| 3 | formal PR integrating RISC-V test profile suite | ① **kernelci-pipeline,唯一主仓交付 = 64 行配置** | ⏳ 就绪未提交 | PR1(纯配置) |
| 4 | runbook/blog/demo/LF badges | ③运营为主 | ⏳ 部分 | Phase 4 补账 |

**当前交付对应**:Phase 2 达成(配置侧代码 + lab 侧实测)、Phase 3 待发 PR。**还差**:①tracking issue(kernelci-project);②PR1 纯配置;③Phase 4(blog/demo/runbook 定稿/badges);④(可选)PR2 worker 或换 tuxrun 后置。无技术债,全是"发出去/讲故事"。

## 3. 下一步怎么做(条目化,标成本与风险)

| # | 动作 | 成本 | 风险 | 推荐顺序 |
|---|---|---|---|---|
| 1 | **tracking issue 开在 kernelci/kernelci-project**(标题对齐"RISC-V pull-lab: qemu-riscv64 kselftest profile → kernelci-pipeline PR"),互链 dev-partners#49 与 xjysiiau 的 #579;#49 回帖挂链接 | 极低(1h) | 低;注意别与 #579 提问重复 | **本周,Phase 1 闭环** |
| 2 | **PR1(纯配置,64 行 4 文件 + pyproject 1 行)**:platforms/jobs/scheduler/pipeline-pull-labs;描述含 validate_yaml.py 输出、callback token 名 `kernelci-pull-labs-riscv` 需运维在 kernelci.toml 配值、勿写 "first ever"(kernelci-core#2008 先例)、如实说明结果回收待上游接线 | 低(半天) | 中:maintainer 可能要求等 tuxrun 通用执行器(#1373 后方向);scheduler 3 条目若 staging 无对应 kbuild-gcc-14-riscv 事件需确认 | **本周** |
| 3 | PR 前把 `src/config_drift.py` / `src/regression_tracker.py` / `doc/regression-tracking.md` 移出 kernelci-pipeline 工作区(或明确 gitignore),避免误入提交集 | 极低 | 低(纯卫生) | PR1 前 |
| 4 | **补一次留证据的复跑**:worker 用 `--output-dir` 跑三类各一遍,存 qemu-test.log + 时长,核实 9/9、7ok/2skip、ssnpm=true | 中(数小时,一次性) | 低;结果若与声称不符需改口径 | PR1 前做,数据贴 PR/runbook |
| 5 | worker 处置(三选一,建议先 5a):**5a 不进 PR,留作 lab 工具**(repo 里放 tools/ 但 PR1 不含,或 self-hosted 仓库);**5b 作 PR2 附送**,PR 说明"example 是演示、不执行 tests[]、无生产 pull-lab 执行器"; **5c 换 tuxrun(方案 A)后置**,前置 = tuxlava 加 kselftest-riscv 类(给上游 PR)+ KVM 表达确认 + lab 装 podman | 5a≈0;5b 中(需论证);5c 高(数天+依赖上游) | 5b 被要求改形态/嫌大(779 vs tools/ 平均)是主要暴露面 | 先 5a,PR1 合入后视 reviewer 反馈再议 5b/5c |
| 6 | 若采纳"删执行层自研":具体删 `serial_reader/drain_pty/run_qemu`(L229–343)、`bake_rootfs_image/mount_cmd` 大半(L148–197)、`guest_bootstrap` 的 mount 部分、三 builder 改参数;**保留** `pollevents/handle_event/poll_loop/save_state/load_state/post_result/error_body/summarize_*` | — | — | 仅在走 5c 时执行 |
| 7 | Phase 4:runbook 定稿(并入 QEMU≥9.1、mkfs.ext4、guest python3、QEMU_BIN 等 lab 要求)→ blog → demo → LF badges | 中(2–3 天) | 低 | Phase 1+3 闭环后 |

## 4. 未验证点与建议补的实验

- **QEMU 声称数值(最重)**:9/9、7ok/2skip、6s/27s/167s、ssnpm=true、v6.19.7-rc1 产物——本地无运行日志,trend-*.md 是 K3 真机且数值不同批。→ 实验 A:worker 带 `--output-dir` 完整复跑并留存日志(见上 #4)。
- **tuxrun 实跑**:dispatcher 镜像大小/可用性、nfsroot `full.rootfs.tar.xz` 直接喂 `--rootfs` 是否被 LAVA deploy 接受、KVM 全链路——均未实跑。→ 实验 B(仅当走 5c):docker pull dispatcher + 用 kernelci storage 现成 artifact 跑一次 baseline;tuxrun 无 boot 测试类,无 `--tests` 的纯 boot 即基线。
- **上游结果回收**:lava_callback 硬编码 lava.Callback、pull_labs.Callback 缺 get_meta → 当前任何执行器回传都接不回来;属上游 future work,PR 里如实声明即可,不阻塞验收。
- 次要:pointer_masking 61 子测试 59/2、vstate_ptrace 是否进 9/9 计数(top-level 8 vs 声称 9)、总结 §7 行数口径(777 vs 实测 779)——对外引用以 01 账本为准。

## 5. 一句话给主代理

**先开 kernelci-project tracking issue、发 PR1 纯配置(都本周),worker 暂留 lab 工具形态、换 tuxrun 作 PR 后置长期项;宣传口径 = "补齐 modular kernelci-pipeline 的 riscv QEMU kselftest 覆盖",数据引用前先补一次留日志复跑。**

---

## 6. 实现记录(方案 A v1 已落地,2026-09)

按方案 A 把 worker 从 779 行减到 **468 行**(-311,约 -40%),换 tuxrun 引擎。

**改动文件**
- `kernelci-pipeline/tools/riscv_pull_worker.py`(重写):保留原协议+健壮性骨架(state=available 轮询、seen/状态文件/flock、整批成功才推游标、回调 POST+error_body、sha256 下载校验),执行层改为映射 job 定义 → `tuxrun` 子进程。删除:PTY/select/threading 串口驱动、`===DONE===` 标记、ctypes mount hack、guest_bootstrap、三套 QEMU builder。tuxrun 在 `linaro/tuxrun-dispatcher` 容器内负责"和 guest 说话"。
- `kernelci-pipeline/tools/tuxlava-kselftest-riscv.patch`(新增):给 tuxlava 补 `kselftest-riscv` 测试类(4 行+,上游 kernelci/tuxrun PR 用)。
- `my-work/tools/riscv_pull_worker_qemu-779.py`(备份):旧自研驱动,需要 KVM 保真度时仍可用。

**Job 映射(实测:三条 argv 均被 tuxrun 1.10.0 解析并渲染出合法 LAVA 定义)**
- boot → `--device qemu-riscv64 --kernel <url> [--rootfs]` + 默认 ext4 buildroot 盘(job 定义只带 cpio ramdisk,qemu device 不认;可用 `--rootfs` 覆盖)。
- kselftest-kvm → `--tests kselftest-kvm` + KSELFTEST overlay 参数 + modules + cpu 自动补 `,h=true`(渲染验证:TST_CMDFILES=kvm、overlay 路径 /opt/kselftests/…)。
- kselftest-riscv → 打补丁后同款(实测注册 + 渲染 `TST_CMDFILES: riscv`);未打补丁时 tuxrun rc=2,worker 识别为 infra error 上报而非假"fail"。

**语义变化与已知限制(如实记录,别当 bug 掩盖)**
1. baseline:从"initramfs ramdisk 引导到 shell"变成"ext4 盘登录"(rootfs 变了,内核 boot 回归信号仍有效)。
2. kselftest 结果粒度:初版曾退化成"整 job pass/fail",真跑发现 tuxrun 对 not ok 也报 pass → 已加 `tap_summary()` 恢复逐 TAP 判读(见 §7),不再依赖 tuxrun 的 rc。
3. KVM:只能整 collection 跑,无法表达 9 项精选子集,也无法先 insmod kvm.ko → 想要原来 7ok/2skip 口径就用备份的 qemu worker。
4. 未实跑:真实运行需 lab 有 podman/docker 且能拉 linaro/tuxrun-dispatcher 镜像(QEMU 在容器内);离线只验证到"参数→LAVA 定义"层。
5. lab 环境要求变更:pip install tuxrun + 容器运行时;不再需要 QEMU 11.1.1/mkfs.ext4 之外的串口工具链(仍要 mkfs.ext4 做 tar→ext4)。

**下一步(如需继续)**:lab 装 podman + tuxrun 后做一次 baseline 真跑验证容器链路;riscv 类补丁作为 kernelci/tuxrun PR 提交;KVM 保真需求决定 worker 是否保留旧驱动双轨。

---

## 7. 真跑验证记录(2026-09-07,worker 468→499 行最终版)

在本人环境(docker + `linaro/tuxrun-dispatcher:latest`,无 podman)用 worker 生成的 argv 真跑了两次,均走完整链路:宿主 tuxrun → dispatcher 容器 → QEMU riscv64 → 内核引导 → auto-login → 测试执行。

**① baseline boot:PASS(rc=0,LAVA `result: pass`,`Job finished correctly`)**
- 内核:本地 kernelci 6.18.0 Image.gz(OpenSBI v1.8 → `Linux version 6.18.0 … gcc 14.2.0` 引导成功,auto-login 匹配 `login:` 并以 root 登录)。rootfs 用 tuxrun 默认 buildroot 盘(先修了宿主机下载截断:curl -C - 预下载后 file:// 传入)。
- 日志:`my-work/evidence/run-logs/real-baseline-boot-pass.log`
- 踩坑:Tuxboot 大文件下载在容器内被截断(Read error 39),改预下载 + bind-mount 绕开——lab 网络对 storage 不稳时要记一笔。

**② kselftest-riscv:真跑执行了 riscv collection,tuxrun 却对失败判 pass——已发现并修复 worker**
- 打了 tools/tuxlava-kselftest-riscv.patch 后,--tests kselftest-riscv 实跑成功:riscv collection 逐项执行(hwprobe/cbo/which-cpus/run_mmap/sigreturn/v_initval/vstate_prctl/pointer_masking),per-test TAP 完整输出。
- **关键发现**:pointer_masking `not ok`(6.18 内核缺修复,预期失败),但 tuxrun/LAVA 仍报 job `pass`(rc=0)→ 纯靠 rc 会漏报回归。已给 worker 加 `tap_summary()`:扫顶层 TAP(`ok/not ok N selftests: riscv: X`),not ok 或 started-but-missing 判 fail。实测对真日志:total 8 / failed 1 / status fail ✓。
- 日志:`my-work/evidence/run-logs/real-kselftest-riscv-run.log`
- 口径说明:此轮结果与总结声称的 9/9 不可比——本机是 6.18.0 内核 + dispatcher 镜像自带 QEMU(非 11.1.1,无 ssnpm),`not ok` 属内核版本预期,不是链路问题。

**③ 未验证项(维持原判断)**:KVM 整 collection 未真跑(需先解决 insmod kvm.ko + 子集表达);真实 callback 收口仍待上游接线;向量/ssnpm 数值需在 QEMU≥9.1 + ssnpm 环境复跑。
