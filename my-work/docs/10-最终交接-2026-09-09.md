# 10｜最终交接(2026-09-09,给下一个会话/agent/未来的自己)

> 先读 09(答疑与行动清单,含 §11 勘误),再读本文件。本文件只分三部分:①交付物在哪、②已验证什么、③还差哪三件事(附环境/坑)。所有"为什么"在 docs/00-10 这 11 份文档里。

## 一、交付物与位置(全部在本机,绝对路径)

**一句话状态**:PR1(4 个 YAML,63 行)就绪未提交;kernelci-pipeline 工作区干净(只剩这 4 个 M);本地官方全栈(API+调度器+worker+真实回调)已端到端跑通(baseline 节点 done/pass)。剩下的三件事是"发出去",不是写代码。

| 东西 | 位置 | 状态 |
|---|---|---|
| PR1 本体 | kernelci-pipeline/config/{platforms,jobs-pull-labs,scheduler-pull-labs,pipeline-pull-labs}.yaml(4 个 M,63 insertions) | 就绪未提交 |
| PR1 补丁副本 | my-work/config/pr1-config.patch | 交付 |
| kernelci-api ssh 容器补丁 | my-work/config/kernelci-api-bullseye-archive.patch | setup-repro.sh 自动应用 |
| worker(现役,tuxrun 版) | my-work/tools/lab/riscv_pull_worker.py | lab 私有 |
| tuxlava 补丁 | my-work/tools/lab/tuxlava-kselftest-riscv.patch | 已打入已装 tuxlava 0.24.0;计划推 kernelci/tuxrun |
| 配置漂移/回归工具 | my-work/tools/lab/config_drift.py、regression_tracker.py、regression-tracking.md | 双模式(本地/生产)可用 |
| 复现入口 | my-work/RUNBOOK.md(档位 A/B)、my-work/tools/fetch-and-run-latest.py、setup-repro.sh、run-local-stack.sh | 实测可用 |
| 文档 | my-work/README.md、docs/(00-10、KernelCI-RISC-V-SOW-总结、文件账本) | 口径已对齐实测 |
| SOW 原文 | my-work/docs/SOW-issue49-body.md | verbatim 存档 |
| 先例铁证 | my-work/evidence/upstream-evidence/(23 个 JSON + verify-report) | #1313/#1357/#1477/#1509/core#3008/#579/#49 全部在线核过 |
| 证据 | my-work/evidence/worker-out/(14GB,gitignore)、evidence/latest-runs/、evidence/run-logs/ | 本地留档 |
| 归档 | my-work/archive/(旧 V1 补丁)、archive/k3-era/(K3 真机全套) | 不删除,不公开携带 |

目录约定(my-work 已按用途分层,不再散落顶层):**docs/** 全部文档、**tools/** 全部工具(lab 在其下)、**config/** 全部补丁与本地配置、**evidence/** 全部证据与运行产物、**archive/** 归档。

## 二、已验证事实卡(对外口径就用这些)

- validate_yaml → "All yaml files are valid";ruff check 全绿;两个 verify 脚本全绿。
- PoC 节点:6a9fb8bfdb925f2f584f0771(done/pass,9-08);重放 6aa11653fa416dea4a15a0bf(done/pass + setup 子节点 + 三构件入库,9-09);防假绿 6aa114ef…(EPERM → incomplete+Infrastructure,如实上报)。
- 官方调度器本地闭环(9-09):种子 kbuild 节点 → scheduler 读我们 4 个 YAML → 自动渲染 3 份任务书(pull_labs_jobs/<日期>/<uuid>.json,存官方 ssh 存储)→ 自动建 3 个 job 节点 → worker 接单 → 真实回调(8003)→ baseline-riscv-pull-labs done/pass;kselftest 两单因 storage.kernelci.org 下载截断如实报 Infrastructure(重跑即可)。
- 最新生产构建 = kbuild-gcc-14-riscv 节点 6aa0b795…(v7.3-rc1-474-g548b86839f7f,2026-09-09);本地 tuxrun 复跑 riscv collection(ssnpm=true)= 10 项 9 ok / 1 not ok(pointer_masking 的 PMLEN constraint,TCG 限制);6.18 上为 7 ok/1 not ok(51/61 SKIP)。
- 漂移:6.18 同 commit 两次构建 5412 项 0 漂移;生产今天两次构建 5583 项 0 漂移;6.18→7.3-rc1 = +274/−103/41。
- 生产 trend(arm64 pull-labs,2303 条)= 2016 pass / 81 fail / 71 regressions。生产 pull-labs-demo 12472 节点(480 incomplete node_timeout / 20 pass / 0 fail)。
- QEMU:宿主 ~/qemu-install = 8.2.0(rvv bug,勿用);tuxrun dispatcher 容器 = 11.0.2(实际执行路径);'11.1.1+ssnpm+9/9 全绿'无原始日志,一律不采信。
- 21 天红线:09-14(#49 最后一条评论 2026-08-24)。

## 三、还差的三件事 + 环境与坑

### 3.1 还差的三件事(按顺序,每件 2 分钟到半天)

1. **#49 回帖**:进度更新(Phase 1-2 done,Phase 3 ready;validate_yaml 通过;PoC 节点 6a9fb8bf…;drift 5412/0;tracking issue 链接;PR 本周)→ 清零 21 天倒计时。文案参考 09 §8。
2. **开 tracking issue**(kernelci/kernelci-project,不是 pipeline):标题 "RISC-V pull-lab: qemu-riscv64 kselftest profile → kernelci-pipeline PR",正文互链 riscv-admin/dev-partners#49 与 kernelci-project#579,附 Status checkbox(见 09 §8 与 02 §2b)。
3. **发 PR1**:fork kernelci/kernelci-pipeline → 分支 riscv-qemu-pull-labs → 只 add 4 个 YAML(严禁 git add -A;pyproject 的 C901 行已随 worker 移入 my-work/tools/lab/ 而撤销,随 worker PR 再带)→ commit "config: add RISC-V QEMU pull-lab test profile" → 描述按 09 §7 口径(首行 Part of … riscv-admin/dev-partners#49;validate_yaml 输出;PoC 节点号;token 值在部署侧 kernelci.toml;声明 tuxlava 补丁存在;禁写 first/回收待接线)。推送前到 GitHub 网页核一眼 #1357/#1477 原始 commit 风格。

### 3.2 环境与服务现状(2026-09-09 晚,仍在运行)

- docker:kernelci-api 五容器(api 8001/mongo 8017/redis/storage 8002/ssh 8022)Up;sentinel 保活容器(保持 docker0 网关可用,别删)。
- 8003 = 真实 lava_callback(uvicorn,KCI_SETTINGS=my-work/config/local-callback.toml,验 token:labtoken-callback);8999 = 构件 http.server(serve my-work/evidence/worker-out/kvm-loop/serve)。
- 本地官方调度器在跑(--runtimes pull-labs-riscv,读我们 4 个 YAML);worker 已停(用时手动起,见 RUNBOOK)。
- 起栈一条命令:bash my-work/tools/run-local-stack.sh [--seed|--worker];本地 admin JWT 在 kernelci-pipeline/.env(gitignored,绝不外传)。

### 3.3 已知坑/诚实清单

- api.kernelci.org 时通时断(今天断过几次,SSL EOF);files.kernelci.org/storage.kernelci.org/github/staging 正常。
- 生产模式工具只读:drift/trend 免 token 可用;track/watch 仅本地 API 可写。
- 144MB rootfs 从 storage.kernelci.org 下载可能截断 → worker 报 Infrastructure,重跑即可。
- Debian bullseye 已归档(官方问题):kernelci-api 的 ssh 容器 Dockerfile 原样 `apt update` 会失败,修复补丁在 my-work/config/kernelci-api-bullseye-archive.patch;setup-repro.sh 会自动打;已装容器不受影响,本地 dev 栈专用,不进任何 PR。
- 根仓库仍有 7 个历史 D(旧顶层文件已移入 my-work/archive/);发布前 git add -u + git add my-work/。.gitignore 已修。
- 补丁 tuxlava-kselftest-riscv.patch 重装 tuxrun 后需重打(patch -p1 到已装包)。

### 3.4 文件地图(新会话读什么)

1. my-work/docs/10(本文档)→ 09(含 §11 勘误)→ 00/02/04/07/08 → RUNBOOK.md → docs/文件账本.md → evidence/upstream-evidence/verify-report。
2. 要动代码前:三个门禁(validate_yaml / verify-worker-guards / verify-lava-body)+ ruff check,全绿才继续。

**给下一个 agent 的一句话:交付物 = PR1(4 文件 63 行,工作区已只剩它)+ 本地证据全套;剩下三件事是发出去,不是写代码;所有"为什么"都在 docs/00-10 这 11 份文档里。**
